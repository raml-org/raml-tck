{
  "section": "semantic/libraries",
  "ramlversion": "1.0",
  "tests": [
    {
      "description": "a root RAML document must not be valid when it references an invalid type from a library",
      "input": "api-with-invalid-lib",
      "valid": false,
      "tags": [
        "type", "library"
      ]
    },
    {
      "description": "a root RAML document must be valid when it references a valid type from a library even when the library contains validation errors",
      "input": "api-with-valid-type-ref",
      "valid": true,
      "tags": [
        "type", "library"
      ]
    }
  ]
}