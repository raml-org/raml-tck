{
  "tests": [
    {
      "description": "explicitly define the type using the `type:` node",
      "input": "explicitly-define-type",
      "valid": true,
      "tags": [
        "type"
      ]
    },
    {
      "description": "implicitly define the type without using the `type:` node which should be equivalent to the explicit form",
      "input": "implicitly-define-type",
      "valid": true,
      "tags": [
        "type"
      ]
    },
    {
      "description": "'usage' must not be a valid node inside a type declaration",
      "input": "invalid-usage-node",
      "valid": false,
      "tags": [
        "type"
      ]
    }
  ]
}